{
  "rules": [
    {
      "rule_id": "MSSQL_WAIT_001",
      "category": "mssql_wait_type",
      "event_pattern": "PAGEIOLATCH_*",
      "condition": "wait_pct_of_total > 15 OR avg_wait_ms > 10",
      "severity": "high",
      "title": "High Buffer I/O Wait (PAGEIOLATCH)",
      "root_cause": "PAGEIOLATCH waits mean SQL Server is waiting on physical disk I/O to bring data pages into the buffer pool. Common causes: buffer pool undersized for the working set (check Page Life Expectancy and Max Server Memory), missing or stale statistics causing more pages to be scanned than necessary, or genuine storage-layer I/O bottleneck.",
      "resolution_steps": [
        "1. Check Page Life Expectancy (mssql_perf_counters, counter_name='Page life expectancy') -- consistently low (under a few hundred seconds) points at buffer pool pressure",
        "2. Check Max Server Memory (mssql_config_snapshot.max_server_memory_mb) against actual physical memory -- an undersized cap forces more physical reads",
        "3. Check mssql_index_usage_delta for the affected database for missing/stale statistics or high leaf-level page churn",
        "4. If storage-layer latency is suspected, check mssql_file_io_delta's io_stall_read_ms for the specific file"
      ],
      "related_rules": ["MSSQL_SEG_001"]
    },
    {
      "rule_id": "MSSQL_WAIT_002",
      "category": "mssql_wait_type",
      "event_pattern": "LCK_M_*",
      "condition": "wait_pct_of_total > 10 OR avg_wait_ms > 50",
      "severity": "high",
      "title": "High Lock Wait (Blocking Contention)",
      "root_cause": "LCK_M_* waits mean sessions are waiting on locks held by other sessions -- genuine blocking, not I/O or CPU pressure. Common causes: long-running transactions holding locks longer than necessary, missing indexes forcing broader lock scope (table/page locks instead of row locks), or a genuinely high-concurrency hot row/page being contended.",
      "resolution_steps": [
        "1. Check mssql_blocking_snapshot for the same time period -- it captures the actual blocking_session_id/wait_resource/request_mode detail this wait-type-level rule can't see on its own",
        "2. Look for long-running transactions (mssql_session_stats' cpu_time_ms/reads high relative to a short session count) that may be holding locks too long",
        "3. Check for missing indexes on the contended table -- a missing index often forces a full scan under a lock that a seek would have avoided",
        "4. Consider READ_COMMITTED_SNAPSHOT isolation if blocking is between readers and writers specifically"
      ],
      "related_rules": []
    },
    {
      "rule_id": "MSSQL_WAIT_003",
      "category": "mssql_wait_type",
      "event_pattern": "CXPACKET",
      "condition": "wait_pct_of_total > 20",
      "severity": "medium",
      "title": "High Parallelism Wait (CXPACKET)",
      "root_cause": "CXPACKET waits mean parallel query threads are waiting on each other to synchronize -- not necessarily a problem on its own (some CXPACKET is normal for genuinely parallel work), but high volume often means Cost Threshold for Parallelism is set too low (parallelizing cheap queries that shouldn't be) or Max Degree of Parallelism is mismatched to the workload.",
      "resolution_steps": [
        "1. Check mssql_config_snapshot.cost_threshold_for_parallelism -- the SQL Server default (5) is very low for most modern workloads; consider raising it (commonly 25-50) if this is genuinely default-configured",
        "2. Check mssql_config_snapshot.max_dop against cpu_count -- an uncapped MAXDOP on a high-core-count server can cause excessive parallelism for small queries",
        "3. Cross-reference with mssql_scheduler_stats' runnable_tasks_count -- if schedulers are already queued, added parallelism is making contention worse, not better"
      ],
      "related_rules": ["MSSQL_SCHED_001"]
    },
    {
      "rule_id": "MSSQL_WAIT_004",
      "category": "mssql_wait_type",
      "event_pattern": "SOS_SCHEDULER_YIELD",
      "condition": "wait_pct_of_total > 15",
      "severity": "high",
      "title": "High CPU Pressure (SOS_SCHEDULER_YIELD)",
      "root_cause": "SOS_SCHEDULER_YIELD waits mean a task voluntarily yielded the CPU to let other tasks run and had to wait for its turn again -- this is a genuine CPU-pressure signal, distinct from I/O or lock waits. High volume means the instance's CPU is a bottleneck for the current workload, not any single query necessarily.",
      "resolution_steps": [
        "1. Check mssql_scheduler_stats' runnable_tasks_count across all schedulers -- consistently high values confirm genuine CPU saturation, not just one busy scheduler",
        "2. Check mssql_plan_cache_stats for high-usecount plans with high total_worker_time_us -- these are the CPU-heaviest recurring queries worth tuning first",
        "3. Check for excessive compilation (mssql_perf_counters, 'SQL Compilations/sec' and 'SQL Re-Compilations/sec') -- parameter sniffing or ad-hoc query volume can itself be a CPU drain independent of the queries' own logic"
      ],
      "related_rules": ["MSSQL_WAIT_003"]
    },
    {
      "rule_id": "MSSQL_WAIT_005",
      "category": "mssql_wait_category",
      "event_pattern": "Buffer IO",
      "condition": "pct_query_wait_time > 15 OR avg_wait_ms > 10",
      "severity": "high",
      "title": "High Buffer I/O Wait — Query Store",
      "root_cause": "This specific query is spending a disproportionate share of its wait time on Buffer IO -- the per-query counterpart to MSSQL_WAIT_001's instance-wide signal, naming the actual query responsible rather than just the aggregate wait type. Same root causes apply (buffer pool pressure, missing/stale statistics, storage latency), but now attributable to one statement worth tuning directly.",
      "resolution_steps": [
        "1. Review this query's plan (mssql_qs_plan) for full/index scans that a covering index could avoid",
        "2. Check whether the tables this query touches have current statistics",
        "3. If the query is fundamentally I/O-bound by its nature (e.g. a genuine full-table reporting scan), confirm that's expected rather than accidental (a missing WHERE clause, for instance)"
      ],
      "related_rules": ["MSSQL_WAIT_001"]
    },
    {
      "rule_id": "MSSQL_WAIT_006",
      "category": "mssql_wait_category",
      "event_pattern": "Lock",
      "condition": "pct_query_wait_time > 10 OR avg_wait_ms > 50",
      "severity": "high",
      "title": "High Lock Wait — Query Store",
      "root_cause": "This specific query is spending a disproportionate share of its wait time waiting on locks -- the per-query counterpart to MSSQL_WAIT_002. Query Store's wait_category granularity can't say which other session or resource is the actual blocker (that detail only exists in mssql_blocking_snapshot, point-in-time, not per-query historically) -- this rule tells you WHICH query is most often the one waiting, not WHY.",
      "resolution_steps": [
        "1. Cross-reference mssql_blocking_snapshot around the times this query ran for the actual blocking_session_id/resource_type/request_mode detail",
        "2. Check the query's own transaction scope -- is it doing more work inside the transaction than strictly necessary, holding locks longer than needed?",
        "3. Check for a missing index that would let this query use a narrower row/page lock instead of a broader table lock"
      ],
      "related_rules": ["MSSQL_WAIT_002"]
    },
    {
      "rule_id": "MSSQL_WAIT_007",
      "category": "mssql_wait_type",
      "event_pattern": "CXSYNC_PORT",
      "condition": "wait_pct_of_total > 20",
      "severity": "medium",
      "title": "High Parallel Exchange Port Synchronization Wait (CXSYNC_PORT)",
      "root_cause": "CXSYNC_PORT (SQL Server 2022+) tracks time spent opening/closing/synchronizing the exchange ports parallel producer and consumer threads use to hand off rows -- introduced specifically to give this signal its own identity separate from CXPACKET. Unlike CXCONSUMER (the benign, expected side of parallelism, deliberately excluded from this rule engine's findings), high CXSYNC_PORT genuinely indicates workload imbalance: sustained port waits most often accompany a blocking operator -- a sort, hash build, or spool -- sitting between producer and consumer threads, so one stage of the plan gates the rest.",
      "resolution_steps": [
        "1. Identify the specific query/plan behind this wait (cross-reference mssql_qs_wait_stats/mssql_qs_plan for the same time window) and look for a blocking operator (Sort, Hash Match, Spool) feeding an exchange -- that operator's input is the real tuning target, not the wait itself",
        "2. Check for skewed or misestimated row counts feeding the blocking operator -- fresh statistics often resolve this",
        "3. Consider whether an index that provides the needed order could avoid the sort entirely",
        "4. Review mssql_config_snapshot.cost_threshold_for_parallelism -- as with CXPACKET (MSSQL_WAIT_003), trivial queries going parallel unnecessarily can contribute"
      ],
      "related_rules": ["MSSQL_WAIT_003"]
    },
    {
      "rule_id": "MSSQL_WAIT_008",
      "category": "mssql_wait_type",
      "event_pattern": "WRITELOG",
      "condition": "wait_pct_of_total > 15 OR avg_wait_ms > 15",
      "severity": "high",
      "title": "High Transaction Log Write Wait (WRITELOG)",
      "root_cause": "WRITELOG waits mean SQL Server is waiting for the transaction log buffer to be flushed to disk -- charged on every COMMIT, on CHECKPOINT, and whenever a log block fills. Two genuinely different root causes produce this signal, distinguishable by which metric is driving the finding: a LOW avg_wait_ms alongside a high wait_pct_of_total points at commit FREQUENCY -- many small, individually-committed transactions each waiting only briefly, but accumulating into a large total simply from volume (batching/commit-frequency issue, not a storage problem). A HIGH avg_wait_ms specifically points at genuine transaction log I/O latency -- the storage subsystem itself is slow to harden each write, regardless of how many there are.",
      "resolution_steps": [
        "1. Check avg_wait_ms specifically: low (a few ms or less) with high volume suggests batching many small transactions into fewer, larger commits, or considering delayed durability (SQL Server 2014+) if the application can tolerate the small window of potential data loss on crash",
        "2. High avg_wait_ms (double digits or more) suggests genuine storage-layer latency -- check mssql_file_io_delta's io_stall_write_ms for the transaction log file specifically, and confirm the log file is on fast, dedicated storage, not shared with data files or other I/O-heavy workloads",
        "3. If using Availability Groups in synchronous commit mode, also check for HADR_SYNC_COMMIT -- the remote log copy can itself introduce this same class of delay, separate from local disk latency",
        "4. Confirm CHECKPOINT frequency isn't excessive (mssql_perf_counters, 'Log Flushes/sec' and related counters) if commits alone don't explain the volume"
      ],
      "related_rules": ["MSSQL_WAIT_009"]
    },
    {
      "rule_id": "MSSQL_WAIT_009",
      "category": "mssql_wait_category",
      "event_pattern": "Tran Log IO",
      "condition": "pct_query_wait_time > 15 OR avg_wait_ms > 15",
      "severity": "high",
      "title": "High Transaction Log Write Wait — Query Store",
      "root_cause": "This specific query is spending a disproportionate share of its wait time on transaction log writes -- the per-query counterpart to MSSQL_WAIT_008's instance-wide signal. Query Store attributes this to whichever query's commit (or the transaction it participates in) triggered the log flush, naming the actual statement responsible rather than just the aggregate wait type.",
      "resolution_steps": [
        "1. Check whether this query commits frequently within a loop or batch (e.g. row-by-row processing with a commit per row) -- consolidating into fewer, larger transactions is often the most direct fix",
        "2. Cross-reference mssql_wait_type's WRITELOG avg_wait_ms (MSSQL_WAIT_008) to determine whether this is a frequency issue (many small commits) or a genuine storage-latency issue affecting this query's commits specifically",
        "3. If this query's transaction includes other write-heavy statements, confirm none of them are generating unexpectedly large log volume (e.g. an unintended full-table UPDATE)"
      ],
      "related_rules": ["MSSQL_WAIT_008"]
    }
  ]
}
